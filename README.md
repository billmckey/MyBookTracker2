ФИО: Буцаев Максим Вадимович 
Группа: СКБ252
Подгруппа: 1
Вариант: Трекер книг

1. Задание:
Написать программу "Трекер книг", которая будет добавлять прочитанные книги и выводить статистику. Статистика включает название книги, дату прочтения, оценку книги   



2. Описание алгоритма:

используем следующие библиотеки:
#define _CRT_SECURE_NO_WARNINGS
#include <iostream> – основа ввода-вывода 
#include <fstream> – работа с файлами 
#include <cstring> – строки в стиле С
#include <iomanip> -красивый вывод(колонки например)
#include <windows.h> – для русского языка
#include <string> – современные строки С++

const int max_books = 1000;  – константа количества книг
struct BOOKS
{
	char date[11];
	char name[40];
	char author[40;]
	float grade;
};
Создадим структуру BOOKS которая хранит информацию о дате, авторе, оценке и названии книг.
Создадим массив для хранения всех книг и счетчик книг.



Функция loadBooks
Создает объект для чтения файла 
Проверяет открылся ли он
Создает временные переменные для чтения в формате string
Читает файл построчно разделяя данные по символу "|" с помощью while
Далее копируем дату, автора, и название  в структуру, выполняя проверки и добавляя конец строки 
Преобразуем оценку в число и увеличиваем счетчик bookkolvo
Закрываем файл

Функция saveBOOKToFIle
Создаем объект для записи в файл
Проверяем открылся ли файл
Записываем все книги в файл через разделитель "|"
Закрываем файл

Функция addBook
Проверяем не превышен ли лимит книг
Созданм новую книгу BOOKS e;
Вводим дату
cin.ignore();   – очищаем буффер обмена              
cin.getline(e.name, 40);  – вводим название
Вводим оценку
Добавляем книгу в массив 
Сохраняем все книги в файл books.txt

Функция showallbooks
Проверяем наличие книг
Выводим таблицу(заголовок и каждую книгу с ее данными)

Функция showgrades
Проверяем наличие книг
Суммируем оценки, находим максимальную и среднюю
Красиво выводим статистику

Функция showmenu
Выводим таблицу с вариантами действий
Главная функция main
Настраиваем язык
загружаем книги из файла
вводим переменную для выбора действия 
Читаем и обрабатываем выбор функции из меню
